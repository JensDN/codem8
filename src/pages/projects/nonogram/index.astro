---
import StarlightPage, {
  type StarlightPageProps,
} from "@astrojs/starlight/components/StarlightPage.astro";
import { NonoGramInlog } from "@/components/react/projects/nonogram.inlog";

const NONOGRAM_PAGE_PROPS: StarlightPageProps = {
  frontmatter: {
    title: "Nonogram",
    tableOfContents: false
  },
  hasSidebar: false,
};
---

<StarlightPage
  frontmatter={NONOGRAM_PAGE_PROPS.frontmatter}
  hasSidebar={NONOGRAM_PAGE_PROPS.hasSidebar}
>
<NonoGramInlog client:load></NonoGramInlog>
  
</StarlightPage>
